{
  "name": "vercel-test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "env": "node ./bin/environment",
    "build:dev": "npm run env add dev && next build && npm run env remove",
    "build:staging": "npm run env add staging && next build && npm run env remove",
    "start": "next start"
  },
  "dependencies": {
    "next": "10.0.8",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
